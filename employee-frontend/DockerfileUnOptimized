# non optimized image
# Large image size,final image contains Final image contains:Node,npm,source code build files node_modules
FROM node:20

WORKDIR /app

# Copy Everything, including node_modules
COPY ..

# Every code change reruns npm install
RUN npm install

# Build tools remain in production image
RUN npm run build
EXPOSE 80
CMD ["npm", "run", "preview"]